 * components:
 *   schemas:
 *     Login:
 *       type: object
 *       required:
 *         - username
 *         - password
 *       properties:
 *         username:
 *           type: string
 *           description: Security personnel's username.
 *         password:
 *           type: string
 *           description: Security personnel's password.
 * 